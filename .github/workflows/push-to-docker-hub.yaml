name: Docker Image CI

on:
  release:
    types: [ published ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Docker Login
      env:
        DOCKER_USERS: ${{ secrets.DOCKER_USER }}
        DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
      run: |
        docker login -u $DOCKER_USER -p $DOCKER_PASSWORD

    - name: Build Docker Image
      run: docker build . --file Dockerfile --tag juanwigg/go-book-api:$GITHUB_REF
    
    - name: Docker Push
      run: docker push ${{secrets.DOCKER_USER}}/go-book-api